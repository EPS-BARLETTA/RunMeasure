RunMeasure — pack complet (QR inclus)
====================================

Ce pack ajoute la génération de QR JSON (compatibles ScanProf) pour :
- Temps intermédiaire
- Demi-Cooper (6′)
- Cooper (12′)

Les QR sont disponibles via un bouton « 📱 Générer le QR JSON » et automatiquement au clic sur « Stop ».
La lib qrcodejs est chargée via deux CDNs (jsDelivr puis unpkg) avec fallback.
